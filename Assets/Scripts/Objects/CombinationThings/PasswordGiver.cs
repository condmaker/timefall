using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PasswordGiver : MonoBehaviour
{
    [SerializeField]
    private PasswordReceiver passwordReceiver;

    [SerializeField]
    private string messageToGive;

    // dunno what to do here... but when this thing is pressed it needs to send the messageToGive to its Receiver

    // Start is called before the first frame update
    private void Start()
    {
        
    }

    // Update is called once per frame
    private void Update()
    {
        
    }
}
